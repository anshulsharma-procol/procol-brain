# Console API contract

**Superseded by [API_CONTRACT.md](API_CONTRACT.md)**, which is the frozen
boundary both sides implement, with `contracts/api.ts` as its TypeScript
source of truth.

This file described an earlier, console-only shape. It is kept only so links
to it still land somewhere useful; nothing implements it.
